# Loaded automatically by `vite`/`npm run dev` (not by `vite build`, which
# runs in production mode) — points the frontend at the standalone FastAPI
# server on its own port during local dev, since they run as two separate
# processes then. No secrets here; safe to commit.
VITE_API_BASE=http://localhost:8000
