# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Regenerated on every dev server run by @nuxt/ui's Vite plugin
# (unplugin-auto-import / unplugin-vue-components), purely for editor
# type-checking — not meant to be hand-edited or committed.
auto-imports.d.ts
components.d.ts

# Names real internal Archive infrastructure — never committed. Path is
# configurable via STACKMAP_SPEC_PATH (see server/env.py); this default
# location is just where local dev expects to find it.
src/stack.yaml

# Python (server/)
.venv
__pycache__
*.pyc
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
